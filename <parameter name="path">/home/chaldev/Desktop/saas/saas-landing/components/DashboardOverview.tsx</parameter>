<parameter name="content">import { UserButton } from '@clerk/nextjs'
import { BookOpen, Clock, Target, TrendingUp } from 'lucide-react'

export default function DashboardOverview() {
  const stats = [
    {
      icon: BookOpen,
      label: 'Active Courses',
      value: '0',
      change: '+0 this week',
      color: 'text-blue-400'
    },
    {
      icon: Clock,
      label: 'Study Hours',
      value: '0h',
      change: '+0h this week',
      color: 'text-green-400'
    },
    {
      icon: Target,
      label: 'Goals Completed',
      value: '0',
      change: '+0 this week',
      color: 'text-purple-400'
    },
    {
      icon: TrendingUp,
      label: 'Progress',
      value: '0%',
      change: '+0% this week',
      color: 'text-orange-400'
    }
  ]

  return (
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
      {stats.map((stat, index) => {
        const Icon = stat.icon
        return (
          <div
            key={index}
            className="bg-slate-900/50 rounded-xl border border-slate-800 p-6 hover:bg-slate-900/70 transition-colors"
          >
            <div className="flex items-center justify-between">
              <div>
                <p className="text-slate-400 text-sm font-medium">
                  {stat.label}
                </p>
                <p className="text-2xl font-bold text-white mt-1">
                  {stat.value}
                </p>
                <p className="text-xs text-slate-500 mt-1">
                  {stat.change}
                </p>
              </div>
              <div className={`p-3 rounded-lg bg-slate-800 ${stat.color}`}>
                <Icon className="h-6 w-6" />
              </div>
          </div>
        )
      })}
    </div>
  )
}</parameter>
